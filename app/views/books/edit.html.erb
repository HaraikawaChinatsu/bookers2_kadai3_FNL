<div class="row">
	<div class="col-md-6 col-md-offset-3">

		<h1>Editing Book</h1>

		<div class="field row">
<%= form_with model: @book, local:true do |f| %>
  <div>Title<%= f.text_field :title, class:"col-xs-12 book_title" %></div>
  <div>Opinion<%= f.text_field :body, class:"col-xs-12 book_body"  %></div>
  <div><%= f.submit "Update Book",  class:"btn btn-primary col-xs-12" %></div>
<% end %>
</div>

		<!--%= render "books/form", book: @book %>-->
		<%= link_to "Show", book_path(@book), class: "book_#{@book.id}" %>
		<%= link_to "Back", books_path, class: "back" %>

	</div>
	<div class="col-md-3"></div>

</div>